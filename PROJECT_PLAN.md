# 프로젝트 계획: 가뭄이 예측될 때, 옥수수를 연료에서 얼마나·어디서 빼야 하는가

**ML(흉작 예측) + 최적화(에탄올 감산 배분)를 결합한 Predict-then-Optimize 파이프라인**

> 이 문서는 팀원 온보딩용입니다. 사전 지식 없이 이 문서만 읽고 프로젝트 전체 그림을
> 이해할 수 있도록 작성했습니다.

---

## 1. 30초 요약

미국은 옥수수의 **약 40%를 자동차 연료(에탄올)** 만드는 데 씁니다. 법(RFS)이 매년
일정량의 에탄올 혼합을 **의무화**하고 있기 때문에, 가뭄으로 옥수수가 흉작이 나도
연료용 소비는 줄지 않고 **부족분의 충격이 전부 사료·식품 가격으로 쏠립니다.**

우리 프로젝트는 두 단계로 이 문제를 다룹니다.

1. **예측 (ML)**: 수확 전(6~8월) 가뭄 지표로 그해 주(state)별 옥수수 생산량을 미리 예측
2. **최적화 (LP)**: 그 예측을 입력으로, 에탄올 의무량을 채우되 **어느 주에서 얼마나
   감산해야 전체 비용(사료·식품 시장 충격 포함)이 최소인지** 배분을 결정

핵심 산출물: **"가뭄 심각도별 → 주별 에탄올 감산 지도 + 최적 감산량 곡선"**

---

## 2. 배경 스토리: 2012년에 실제로 있었던 일

- 2012년, 미국 반세기 만의 최악 가뭄. 옥수수 수확량이 추세 대비 ~26% 폭락, 가격은
  사상 최고($8+/부셸)로 폭등.
- 축산업계·주지사들·UN까지 나서서 "올해만이라도 에탄올 의무량을 면제해달라"고
  EPA(환경보호청)에 공식 청원.
- **EPA는 거부**했는데, 거부 사유가 "면제의 효과가 정량적으로 입증되지 않았다"였음.
  즉, **판단에 필요한 계산 도구가 그때 없었음.**
- 한편 시장에서는 옥수수값을 감당 못 한 에탄올 공장 수십 곳이 실제로 가동을 중단.
  어느 공장이 서는지는 지역별 옥수수 사정에 따라 갈렸음 — **"감산을 어디서 하느냐"는
  실존하는 의사결정임.**

우리는 "그때 있었어야 할 도구"를 만들고, 2012년 데이터로 백테스트합니다.

---

## 3. 왜 이 주제가 수업 요구사항에 맞는가 (심사 대응표)

수업에서 요구하는 "진짜 ML+최적화 결합"의 조건별 대응:

| 요구 조건 | 우리 설계 |
|---|---|
| **반복적으로 선택하는 의사결정자가 있을 것** | 에탄올 업계는 매년/분기마다 공장별 가동률을 결정 (2012-13년 수십 개 공장 중단이 실제 사례). EPA도 매년 의무량(RVO)을 설정 |
| **희소한 자원이 있어 전부 다 해줄 수 없을 것** | 흉작 해에는 주별 옥수수 공급이 부족 → 의무량은 채워야 하는데 **모든 주에서 공장을 다 돌릴 수 없음.** 어딘가는 감산해야 하고, 어디서 감산하느냐에 따라 비용이 달라짐 |
| **자원 한도 파라미터는 데이터셋에 없으므로 직접 조사** | ① 주별 에탄올 공장 생산능력 (RFA/EIA 공개 목록) ② 옥수수→에탄올 전환율 (1부셸 ≈ 2.8갤런) ③ 연도별 의무량 (EPA RVO 연혁) ④ 주별 사료 수요 하한 (축산 사육두수 기반) |
| **ML 예측값이 비용·가치 계수로 최적화에 들어갈 것** | 가뭄지수로 예측한 **주별 옥수수 생산량**이 각 주의 공급 제약 및 조달비용 계수로 LP에 직접 투입됨. 예측이 바뀌면 최적 배분이 바뀜 |
| **목적함수는 통계 지표가 아니라 운영 지표일 것** | RMSE/F1이 아니라 **"의무량 충족의 총비용(조달 + 사료시장 압박 비용) 최소화"** |

---

## 4. 파이프라인 설계

```
[가뭄지수·기후 데이터 (생육기)]
        │
        ▼
┌─ 1단계: ML 예측 ─────────────────┐
│ 주별 옥수수 생산량 예측           │  ← 기존 model.py 골격 재사용
│ (추세 대비 편차 %)                │     선행연구 인용으로 방법론 정당화
└──────────────┬───────────────────┘
               │ 예측 생산량 = LP의 공급 제약·비용 계수
               ▼
┌─ 2단계: LP 최적화 ───────────────┐
│ 결정변수: q[주] = 주별 에탄올용   │  ← 기존 optimizer.py (PuLP) 골격 재사용
│           옥수수 사용량           │
│ 제약: ① Σq ≥ 의무량×(1-X)        │     X = EPA 면제율 (시나리오 파라미터)
│       ② q[주] ≤ 주별 공장능력    │
│       ③ 주별 수지: 에탄올+사료   │
│          하한 ≤ 예측생산+반입    │
│ 목적: 조달·수송비 + 사료시장     │
│       압박비용 최소화             │
└──────────────┬───────────────────┘
               ▼
[산출물: 가뭄 심각도별 주별 감산 지도,
 면제율 X에 따른 감산 부담 재배분 분석]
```

**predict-then-optimize 연결이 핵심**: 평년 시나리오와 가뭄 시나리오를 1단계에 넣으면
예측 생산량이 달라지고, 그 결과 2단계의 최적 감산 배분이 달라집니다. "예측이 실제
의사결정을 바꾼다"를 정량적으로 보여주는 구조입니다.

---

## 5. 검증: 2012년 백테스트 (프로젝트 하이라이트)

1. 2012년 **7월까지의 가뭄 데이터만** 모델에 입력 (그 뒤 데이터는 봉인 — 실제 EPA가
   결정해야 했던 시점 재현)
2. 모델이 흉작 규모를 예측하고 → 주별 감산 배분과 권고 면제율을 출력
3. **실제 결과와 대조**: 2012-13년에 실제로 가동 중단된 에탄올 공장들의 지역 분포와
   모델이 감산을 권고한 지역이 일치하는지 확인
4. 기존 연구 논쟁(Carter "가격효과 40%" vs 퍼듀 "불확실")을 RIN 재고·탄력성 파라미터를
   바꿔가며 감도분석으로 재현

---

## 6. 데이터 계획

| 데이터 | 용도 | 출처 | 상태 |
|---|---|---|---|
| 가뭄지수 (주별·주간, 2000-2023) | ML 피처 | US Drought Monitor | ✅ **실제 데이터 확보 완료** (`data/raw/drought_monitor.csv`) |
| 주별 옥수수 생산량·수확량 | ML 타깃 | USDA NASS Quick Stats | 다운로드 필요 |
| 주별 에탄올 공장 생산능력 | LP 제약 | RFA / EIA 공개 목록 | 조사 필요 (작은 표) |
| 연도별 에탄올 의무량 (RVO) | LP 제약 | EPA 공표 자료 | 조사 필요 (작은 표) |
| 주별 사료 수요 하한 | LP 제약 | NASS 축산 사육두수 | 다운로드 필요 |
| 수요탄력성, RIN 메커니즘 | 시장 파라미터 | 선행연구 문헌값 | 논문 인용 (조사 필요) |

> **주의**: 개발 환경(샌드박스)에서 usda.gov 등 정부 사이트 접속이 차단되어 있어,
> 데이터는 팀원이 로컬에서 다운로드해 레포에 넣는 방식으로 진행합니다.
> (가뭄 데이터도 이 방식으로 이미 확보했음)

---

## 7. 선행연구와 우리의 기여 (신규성 방어 논리)

**이미 연구된 것 (우리는 인용해서 사용):**
- 기후→작물 손실 예측이 가능하다는 것: arXiv:2408.02217 (신경망으로 기후변화 시
  옥수수 보험청구 확률 2배 예측) 등
- 2012년 면제가 가격에 미쳤을 효과의 **사후** 추정: Carter et al. (~40% 효과),
  퍼듀대 연구 (RIN 재고에 따라 불확실), UNL 2015 등 — 결과가 서로 충돌함

**아직 없는 것 (우리의 기여):**
1. 기존 연구는 전부 **사후(retrospective) 분석** — 가뭄이 끝난 뒤 계산. 우리는
   **수확 전 예측 기반의 사전(ex-ante) 결정 도구**를 만듦 (EPA가 8~10월에 실제로
   필요했던 형태)
2. 아무도 **최적화 문제로 정식화하지 않았음** — "흉작 심각도별 최적 감산 배분"을
   LP로 푸는 것은 우리가 처음
3. 기존 연구들이 충돌한 원인(RIN 재고, 탄력성 가정)을 **명시적 입력 파라미터**로
   노출해, 가정에 따라 결론이 어떻게 변하는지 투명하게 보여주는 재현 가능한
   오픈소스 모델

**설계 원칙 하나**: 기존 연구의 결론 중 "EPA는 가뭄 손실 총량을 줄일 수 없고, 누가
떠안을지 **재분배**할 수 있을 뿐"이라는 통찰을 정면 수용 — 이해관계자(축산·에탄올·
소비자) 가중치를 목적함수의 명시적 파라미터로 두고, 가중치 선택별 최적해를 비교한다.

---

## 8. 역할 분담 (4갈래)

| 역할 | 내용 | 산출물 |
|---|---|---|
| ① 데이터·예측 | NASS 데이터 확보, 주별 생산량 예측 모델 (기존 코드 개조) | 예측 모델 + 성능 리포트 |
| ② 파라미터 조사 | 공장능력·RVO 연혁·전환율·사료 하한·탄력성 문헌값 | 파라미터 표 + 출처 문서 |
| ③ 최적화 | LP 정식화·PuLP 구현 (기존 optimizer.py 재설계) | 최적화 모듈 + 정식화 문서 |
| ④ 시나리오·검증 | 2012 백테스트, 감도분석, 시각화 | 백테스트 리포트 + 그림 |

---

## 9. 정직하게 명시할 한계

- 시장 모듈은 구조적 균형모형이 아니라 **탄력성 기반 근사** (부분균형, 옥수수 시장만)
- 대두 등 대체작물로의 파급 효과는 범위 밖
- DDGS(에탄올 부산물이 사료로 되돌아오는 효과)는 포함 예정이나 단순화된 형태
- 공장 단위가 아닌 주 단위 집계 (공장별 데이터는 비공개가 많음)

---

## 10. 기존 코드 재사용 맵 (이 레포의 이전 버전에서)

이 레포는 원래 "기후 시나리오별 작물 배치 최적화" 프로젝트였고, 주제를 피벗하면서
아래와 같이 재사용합니다.

| 기존 모듈 | 처리 |
|---|---|
| `src/model.py` (주별 수확량 ML 예측) | **개조 재사용** — 타깃·피처 구성 거의 동일 |
| `src/optimizer.py` (PuLP 주별 배분 LP) | **재설계 재사용** — 배분 구조 유사, 목적함수·제약 교체 |
| `src/visualize.py`, 노트북 3개 | **개조 재사용** |
| `data/raw/drought_monitor.csv` | **그대로 사용** (실제 USDM 데이터) |
| `src/generate_synthetic_data.py`, 합성 수확량·기후 데이터 | **폐기** (실데이터로 교체) |

---

## 11. 다음 단계 (순서대로)

1. 팀 전원이 이 문서 리뷰 → 설계 확정
2. 데이터 확보: NASS 옥수수 생산량 + 축산 사육두수 다운로드, 파라미터 조사 시작
3. 레포 리팩터: README 리프레이밍, 모듈 교체
4. 예측 모델 → 최적화 → 백테스트 → 시각화 순서로 구현
5. 최종: 노트북·발표자료 정리
