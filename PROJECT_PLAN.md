# 프로젝트 계획: 가뭄이 예측될 때, 옥수수를 연료에서 얼마나·어디서 빼야 하는가

**ML(흉작 예측) + 최적화(에탄올 감산 배분)를 결합한 Predict-then-Optimize 파이프라인**

> 이 문서는 팀원 온보딩용입니다. 사전 지식 없이 이 문서만 읽고 프로젝트의 배경,
> 전체 구조, 수리 모형까지 이해할 수 있도록 작성했습니다.

---

## 1. 30초 요약

미국은 옥수수의 **약 40%를 자동차 연료(에탄올)** 만드는 데 씁니다. 법(RFS,
Renewable Fuel Standard)이 매년 일정량의 에탄올 혼합을 **의무화**하고 있기 때문에,
가뭄으로 옥수수가 흉작이 나도 연료용 소비는 줄지 않고 **부족분의 충격이 전부
사료·식품 가격으로 쏠립니다.**

우리 프로젝트는 두 단계로 이 문제를 다룹니다.

1. **예측 (ML)**: 수확 전(6~8월) 가뭄 지표로 그해 주(state)별 옥수수 생산량을 미리 예측
2. **최적화 (LP)**: 그 예측을 입력으로, 에탄올 의무량을 채우되 **어느 주에서 얼마나
   감산해야 전체 비용(사료·식품 시장 충격 포함)이 최소인지** 배분을 결정

핵심 산출물: **"가뭄 심각도별 → 주별 에탄올 감산 지도 + 최적 면제율 곡선"**

---

## 2. 프로젝트 선정 배경 (어떻게 이 주제에 도달했나)

이 주제는 한 번에 나온 것이 아니라, 여러 후보를 검토하고 기각하면서 좁혀진
결과입니다. 발표 때 "왜 이 주제인가"에 대한 방어 논리가 되므로 과정을 기록해둡니다.

**출발점 — 수업 요구사항**: "예측 모델을 만드는 데서 끝나지 말고, 그 예측을 입력으로
받아 제한된 자원을 배분하는 의사결정까지 이어져야 한다." 구체적으로 다섯 가지 조건:
① 반복적으로 선택하는 의사결정자가 있을 것, ② 예산·인력·슬롯처럼 희소한 자원이
있어 전부 다 해줄 수 없을 것, ③ 자원 한도 같은 파라미터는 데이터셋에 없으므로 직접
조사할 것, ④ ML 예측값이 비용·가치·위험 계수로 최적화에 들어갈 것, ⑤ 목적함수는
F1·RMSE 같은 통계 지표가 아니라 운영 지표일 것.

**1차 시도 — 기후 기반 작물 배치 최적화 (이 레포의 초기 버전)**: 기후·가뭄 데이터로
작물별 수확량을 예측하고, 토지·물 제약 하에서 미국 전체 생산량을 최대화하는 작물
배치를 LP로 풀었다. **기각 사유**: 미국은 곡물 과잉생산·수출국이라 "총생산 극대화"가
실제로 절실한 문제가 아니고, 전국 작물 배치를 결정하는 의사결정자도 존재하지 않는다
(시장이 결정함). 기술적으로는 완성했지만 동기가 약했다.

**검토했다 기각한 대안들**:
- *오갈랄라 대수층 지역의 관개→건조농법 전환 시점 최적화*: 문제는 실재하지만
  (관개농업이 그 지역 지하수 취수의 90%+, 대수층 고갈 진행 중), 수혜자가 서부
  캔자스·텍사스 팬핸들의 관개 농가 수천 명으로 좁아 "소수의 사적 이익을 위한
  프로젝트"라는 비판을 넘기 어려웠다.
- *연방 작물보험 보조금 예산 배분*: 루브릭에는 가장 깔끔하게 맞았지만, 예측 파트가
  이미 활발한 연구 영역(레드오션)이고 팀의 관심사가 옥수수-에너지 문제 쪽이었다.

**최종 선택의 계기 — 2012년 EPA 면제 거부 사건**: 조사 과정에서 "의사결정 도구가
없어서 실제 정책 결정이 막혔던" 실증 사례를 발견했다 (아래 3절). 게다가 후속
연구들을 확인해보니 전부 가뭄이 끝난 뒤의 **사후 분석**이었고, EPA가 실제로 필요했던
**"수확 전 예측에 기반한 사전 결정 도구"는 지금도 없다**는 공백이 확인됐다.
마지막으로, "면제율 X%를 정한다"는 스칼라 결정만으로는 루브릭의 ②(희소 자원 배분)가
약하다는 지적을 반영해, **"감산을 어느 주에서 얼마나 하느냐"라는 배분 문제**로 한
단계 구체화하면서 다섯 조건이 모두 충족되는 현재 설계에 도달했다.

---

## 3. 배경 스토리: 2012년에 실제로 있었던 일

- 2012년, 미국 반세기 만의 최악 가뭄. 옥수수 수확량이 추세 대비 ~26% 폭락, 가격은
  사상 최고($8+/부셸)로 폭등.
- 축산업계·주지사들·UN까지 나서서 "올해만이라도 에탄올 의무량을 면제해달라"고
  EPA(환경보호청)에 공식 청원. RFS법은 EPA에 면제(waiver) 권한을 부여하고 있음.
- **EPA는 거부**했는데, 거부 사유가 "면제의 효과가 정량적으로 입증되지 않았다"였음.
  즉, **판단에 필요한 계산 도구가 그때 없었음.**
- 한편 시장에서는 옥수수값을 감당 못 한 에탄올 공장 수십 곳이 실제로 가동을 중단.
  어느 공장이 서는지는 지역별 옥수수 사정에 따라 갈렸음 — **"감산을 어디서 하느냐"는
  실존하는 의사결정임.**

우리는 "그때 있었어야 할 도구"를 만들고, 2012년 데이터로 백테스트합니다.

---

## 4. 심사 대응표 (수업 요구조건 5개)

| 요구 조건 | 우리 설계 |
|---|---|
| **① 반복적으로 선택하는 의사결정자** | 에탄올 업계는 매년/분기마다 공장별 가동률을 결정 (2012-13년 수십 개 공장 중단이 실제 사례). EPA도 매년 의무량(RVO)을 설정하고 면제 권한을 보유 |
| **② 희소한 자원, 전부 다 해줄 수 없음** | 흉작 해에는 주별 옥수수 공급이 부족 → 의무량은 채워야 하는데 **모든 주에서 공장을 다 돌릴 수 없음.** 어디서 감산하느냐에 따라 비용이 달라짐 |
| **③ 자원 한도 파라미터는 직접 조사** | 주별 에탄올 공장능력(RFA/EIA), 전환율(1부셸≈2.8갤런), 연도별 의무량(EPA RVO), 주별 사료 수요 하한(사육두수 기반), RIN 이월 재고 — 전부 원본 데이터셋에 없어 별도 조사 |
| **④ ML 예측값이 계수로 투입** | 가뭄지수로 예측한 **주별 옥수수 생산량 Ŷᵢ**가 각 주의 공급 제약 우변과 조달비용 계수에 직접 들어감. 예측이 바뀌면 최적 배분이 바뀜 |
| **⑤ 운영 지표 목적함수** | RMSE/F1이 아니라 **"의무량 충족의 총 사회비용(조달비 + 수송비 + 사료시장 압박비용) 최소화"** |

---

## 5. 1단계 — ML 예측 모형

**목적**: 수확(10~11월) 전, 생육기(6~8월)까지 관측된 정보만으로 그해 주별 옥수수
생산량을 예측한다. "사전(ex-ante) 결정 도구" 스토리의 생명선이므로, **수확 이후에만
알 수 있는 변수는 피처에서 배제**한다.

- **입력 (피처)**: 생육기 가뭄지수(주별·주간 D0~D4 가중 평균), 기온·강수, 재배면적,
  기술 추세(연도)
- **출력 (타깃)**: 주별 옥수수 생산량 — 실무적으로는 "추세 대비 편차 %"로 정규화
  (기술 발전에 의한 장기 상승 추세를 제거해 가뭄 충격만 학습)
- **모델**: Random Forest / XGBoost / LightGBM / CatBoost 비교, 연도 기준 시계열 분할
  검증 (예: 2018년 이전 학습 / 이후 테스트). 기존 `src/model.py` 골격 재사용
- **방법론 정당화**: 기후→작물 손실 예측이 가능하다는 것은 선행연구로 이미 입증
  (arXiv:2408.02217 등) — 예측 방법의 신규성은 주장하지 않고 인용으로 대체

예측 결과 **Ŷᵢ** (주 i의 예측 생산량)가 2단계 최적화의 입력이 된다.

---

## 6. 2단계 — 최적화 모형 (완전 정식화)

### 6.1 문제 구조: 2층 구조

- **안쪽 (LP, 배분 문제)**: 면제율 X가 주어졌을 때, 의무량을 채우면서 총비용을
  최소화하는 **주별 에탄올용 옥수수 사용량 배분**
- **바깥쪽 (정책 곡선)**: X를 0~30% 격자로 훑으며 안쪽 LP를 반복해 풀고, X별 사회
  비용을 비교해 **최적 면제율 X\*** 를 찾는다. 산출물이 "흉작 심각도 → X\* 곡선"

### 6.2 집합과 인덱스

| 기호 | 의미 |
|---|---|
| `i ∈ S` | 옥수수 주산지 주. 기존 12개 주(IA, IL, NE, MN, IN, KS, OH, SD, MO, ND, WI, TX) — 미국 옥수수 생산의 ~85% |

### 6.3 파라미터 (전부 출처 명시 — 루브릭 ③ 대응)

| 기호 | 의미 | 단위 | 출처 |
|---|---|---|---|
| `Ŷᵢ` | **1단계 ML이 예측한** 주 i의 옥수수 생산량 | bu | 우리 예측 모델 (루브릭 ④) |
| `Cᵢ` | 주 i의 에탄올 공장능력 (옥수수 환산) | bu/년 | RFA/EIA 공장 목록의 갤런 능력 ÷ 2.8 |
| `M` | 그해 옥수수 에탄올 의무량 | gal | EPA RVO 연혁 |
| `X` | 면제율 (시나리오/정책 변수) | [0, 1] | 정책 레버 — 바깥층에서 격자 탐색 |
| `K` | RIN 이월 크레딧으로 대체 가능한 물량 | gal | EPA EMTS 자료·문헌 (퍼듀 연구의 핵심 변수) |
| `γ` | 전환율 (옥수수→에탄올) | 2.8 gal/bu | 업계 표준값 |
| `Fᵢ` | 주 i의 사료 최소 수요 | bu | NASS 사육두수 × 두당 옥수수 소요량 |
| `E` | 전국 수출 최소 물량 (기계약·추세 하한) | bu | USDA ERS 수출 통계 |
| `cᵢ` | 주 i의 옥수수 조달 단가 (예측 공급 부족이 심한 주일수록 높음) | $/bu | 기준가 × 지역 부족도 프리미엄 (Ŷᵢ의 함수) |
| `τ` | 주간 반입(수송) 단가 | $/bu | 곡물 수송비 조사값 |
| `λ` | 사료 하한 잠식 페널티 단가 | $/bu | 사료 수요탄력성 문헌값으로 보정 |
| `σ` | 사료 하한 중 잠식 허용 비율 상한 | [0,1] | 설계 파라미터 (감도분석 대상) |
| `u` | 공장 최소 가동률 (MIP 확장 시) | [0,1] | 업계 자료 (부분가동 불가 특성) |

### 6.4 결정변수

| 기호 | 의미 | 범위 |
|---|---|---|
| `qᵢ` | 주 i에서 에탄올 생산에 투입하는 옥수수 | ≥ 0 |
| `tᵢ⁺, tᵢ⁻` | 주 i로의 반입량 / 반출량 (주간 이동) | ≥ 0 |
| `sᵢ` | 주 i의 사료 하한 잠식량 (슬랙, 페널티 부과) | ≥ 0 |
| `yᵢ` | (MIP 확장) 주 i 공장 가동 여부 | {0, 1} |

### 6.5 목적함수 (안쪽 LP)

**의무량 충족의 총비용 최소화** — 운영 지표 (루브릭 ⑤):

```
min  Σᵢ cᵢ·qᵢ          (에탄올용 옥수수 조달비 — 부족한 주에서 뽑을수록 비쌈)
   + Σᵢ τ·tᵢ⁺          (주간 수송비)
   + Σᵢ λ·sᵢ           (사료 시장 압박 비용 — 사료 하한을 잠식한 만큼 페널티)
```

ML 예측 Ŷᵢ는 두 경로로 목적함수·제약에 들어간다: ① cᵢ가 예측 부족도의 함수
(흉작 예측이 심한 주일수록 그 주에서 옥수수를 빼는 비용이 커짐), ② 아래 제약 (3)의
우변 (그 주에서 뺄 수 있는 물리적 한도).

### 6.6 제약조건

```
(1) 의무량 충족:      γ·Σᵢ qᵢ  ≥  (1−X)·M − K
    — 면제율 X만큼 줄고, RIN 이월 크레딧 K만큼 물리적 혼합을 대체 가능.
      K가 크면 면제해도 실효가 없다는 퍼듀 연구의 논점이 이 항에서 재현됨.

(2) 공장능력:         qᵢ  ≤  Cᵢ                                  ∀i
    — 각 주는 설치된 공장능력 이상 생산 불가.

(3) 주별 옥수수 수지: qᵢ + Fᵢ − sᵢ + tᵢ⁻  ≤  Ŷᵢ + tᵢ⁺           ∀i
    — 에탄올 사용 + 사료 수요(잠식분 제외) + 반출 ≤ 예측 생산 + 반입.
      ML 예측 Ŷᵢ가 우변에 직접 등장 (루브릭 ④의 핵심).

(4) 전국 수지·수출:   Σᵢ tᵢ⁺ = Σᵢ tᵢ⁻ ,   Σᵢ (Ŷᵢ − qᵢ − Fᵢ + sᵢ)  ≥  E
    — 주간 이동은 총량 보존, 수출 최소 물량은 전국 단위로 보장.

(5) 사료 잠식 상한:   sᵢ  ≤  σ·Fᵢ                                ∀i
    — 사료 수요를 무한정 희생할 수 없음 (축산 구조조정의 물리적 한계).

(6) 비음조건:         qᵢ, tᵢ⁺, tᵢ⁻, sᵢ  ≥  0                     ∀i

(7) [MIP 확장] 최소가동률:  u·Cᵢ·yᵢ ≤ qᵢ ≤ Cᵢ·yᵢ ,  yᵢ ∈ {0,1}   ∀i
    — 에탄올 공장은 일정 가동률 이하로는 운영 불가 → "부분 감산"이 아니라
      "가동/중단" 결정이 되는 현실 반영. LP 기본형 완성 후 확장.
```

**왜 이 제약 조합인가**: 목적함수가 비용 최소화뿐이면 최적해는 "옥수수가 남는
주에서만 전부 생산"으로 시시하게 수렴한다. (2) 공장능력 상한과 (3) 주별 수지,
(5) 사료 하한이 **동시에 binding**되도록 파라미터를 설정해야 "부족한 주에서도 어느
정도 생산할 수밖에 없는" 의미 있는 트레이드오프가 발생한다. 이는 이전 버전
프로젝트(작물 배치 최적화)에서 검증했던 설계 원칙을 그대로 계승한 것이다.

### 6.7 바깥층 — 최적 면제율 X\* 결정

X를 0%~30% 격자로 훑으며 안쪽 LP를 풀고, X별 **사회비용**을 비교한다:

```
SC(X) =  w₁ · [사료·식품 가격충격]      ≈ (사료용 공급 감소율 ÷ 수요탄력성 ε) × 사료 소비액
       + w₂ · [연료 측 추가비용]        ≈ 면제된 갤런 × (에탄올 대체 비용 차이 또는 RIN 가격)
       + w₃ · [에탄올 산업 손실]        ≈ 감산 갤런 × 산업 마진

X* = argmin SC(X)
```

- 탄력성 ε, RIN 가격 등은 선행연구 문헌값을 인용하고 **감도분석 대상**으로 명시
- **가중치 w₁, w₂, w₃는 가치판단**이다. 기존 연구의 결론 — "EPA는 가뭄 손실 총량을
  줄일 수 없고 누가 떠안을지 **재분배**할 수 있을 뿐" — 을 정면 수용하여, 가중치를
  숨기지 않고 명시적 파라미터로 노출한다. 이해관계자(축산·에탄올·소비자) 관점별로
  X\*가 어떻게 달라지는지 비교하는 것 자체가 분석 결과물이다.

### 6.8 Predict-then-Optimize 연결 고리 (한눈에)

```
[생육기 가뭄지수·기후] ──ML──▶ Ŷᵢ (주별 예측 생산량)
                                 │
                 ┌───────────────┼────────────────┐
                 ▼               ▼                ▼
           제약(3) 우변      조달단가 cᵢ      바깥층 SC(X)의
           (물리적 한도)     (부족 프리미엄)   가격충격 항
                 └───────────────┼────────────────┘
                                 ▼
                     안쪽 LP: 주별 감산 배분 q*
                                 ▼
                     바깥층: 최적 면제율 X* 곡선
```

가뭄 시나리오(평년 vs 2012년급)를 바꾸면 Ŷᵢ가 바뀌고, 그에 따라 q\*와 X\*가 바뀐다.
"예측이 실제 의사결정을 바꾼다"가 정량적으로 시연되는 구조.

---

## 7. 검증: 2012년 백테스트 (프로젝트 하이라이트)

1. 2012년 **7월까지의 가뭄 데이터만** 모델에 입력 (그 뒤 데이터는 봉인 — 실제 EPA가
   결정해야 했던 8~10월 시점을 재현)
2. 모델이 흉작 규모를 예측하고 → 주별 감산 배분 q\*와 권고 면제율 X\*를 출력
3. **실제 결과와 대조**: 2012-13년에 실제로 가동 중단된 에탄올 공장들의 지역 분포와
   모델이 감산을 권고한 지역이 일치하는지 확인
4. 기존 연구 논쟁 재현: Carter et al.("면제 시 가격 40% 하락 효과") vs 퍼듀("RIN 재고
   때문에 불확실")의 충돌을, K(RIN 재고)와 ε(탄력성) 파라미터를 바꿔가며 감도분석으로
   재현 — "가정에 따라 결론이 달라지는 지점"을 투명하게 보여줌

---

## 8. 데이터 계획

| 데이터 | 모형에서의 역할 | 출처 | 상태 |
|---|---|---|---|
| 가뭄지수 (주별·주간, 2000-2023) | ML 피처 | US Drought Monitor | ✅ **실제 데이터 확보 완료** (`data/raw/drought_monitor.csv`) |
| 주별 옥수수 생산량·수확량·재배면적 | ML 타깃, Ŷᵢ 검증 | USDA NASS Quick Stats | 다운로드 필요 |
| 생육기 기온·강수 | ML 피처 | NOAA (또는 가뭄지수만으로 시작) | 다운로드 필요 (우선순위 낮음) |
| 주별 에탄올 공장능력 `Cᵢ` | LP 제약 (2) | RFA / EIA 공개 목록 | 조사 필요 (작은 표) |
| 연도별 의무량 `M` | LP 제약 (1) | EPA RVO 공표 자료 | 조사 필요 (작은 표) |
| RIN 이월 재고 `K` | LP 제약 (1) | EPA EMTS·문헌 | 조사 필요 |
| 주별 사료 수요 하한 `Fᵢ` | LP 제약 (3),(5) | NASS 축산 사육두수 | 다운로드 필요 |
| 수출 하한 `E` | LP 제약 (4) | USDA ERS | 조사 필요 |
| 수요탄력성 `ε`, 수송단가 `τ`, 마진 | 목적함수·SC(X) | 선행연구 문헌값 | 논문에서 인용 |

> **주의**: 개발 환경(샌드박스)에서 usda.gov 등 정부 사이트 접속이 차단되어 있어,
> 데이터는 팀원이 로컬에서 다운로드해 레포에 넣는 방식으로 진행합니다.
> (가뭄 데이터도 이 방식으로 이미 확보했음)

---

## 9. 선행연구와 우리의 기여 (신규성 방어 논리)

**이미 연구된 것 (우리는 인용해서 사용):**
- 기후→작물 손실 예측이 가능하다는 것: arXiv:2408.02217 (신경망, 기후변화 시 옥수수
  보험청구 확률 2배) 등
- 2012년 면제가 가격에 미쳤을 효과의 **사후** 추정: Carter, Rausser & Smith (~40%
  효과), 퍼듀대 Tyner et al. (RIN 재고에 따라 불확실), UNL 2015, EPA 자체 리뷰 —
  **결과가 서로 충돌함**

**아직 없는 것 (우리의 기여):**
1. 기존 연구는 전부 **사후(retrospective) 분석**. 우리는 **수확 전 예측 기반의
   사전(ex-ante) 결정 도구** (EPA가 2012년 8~10월에 실제로 필요했던 형태)
2. 아무도 **배분 최적화 문제로 정식화하지 않았음** — "의무량 감축분을 어느 주에서
   어떻게 흡수하는 게 비용 최소인가"를 LP/MIP로 푸는 것은 우리가 처음
3. 기존 연구들이 충돌한 원인(RIN 재고 K, 탄력성 ε)을 **명시적 입력 파라미터**로
   노출해, 가정→결론 관계를 투명하게 재현하는 오픈소스 모델

---

## 10. 역할 분담 (4갈래)

| 역할 | 내용 | 산출물 |
|---|---|---|
| ① 데이터·예측 | NASS 데이터 확보, 주별 생산량 예측 모델 (기존 코드 개조) | 예측 모델 + 성능 리포트 |
| ② 파라미터 조사 | 공장능력·RVO·전환율·사료 하한·RIN·탄력성 문헌값 (6.3절 표의 "조사 필요" 항목 전부) | 파라미터 표 + 출처 문서 |
| ③ 최적화 | 6.5~6.7절 정식화의 PuLP 구현 (기존 optimizer.py 재설계) | 최적화 모듈 + 정식화 문서 |
| ④ 시나리오·검증 | 2012 백테스트, 감도분석, 시각화 | 백테스트 리포트 + 그림 |

---

## 11. 정직하게 명시할 한계

- 시장 모듈은 구조적 균형모형이 아니라 **탄력성 기반 근사** (부분균형, 옥수수 시장만)
- 대두 등 대체작물로의 파급 효과는 범위 밖
- DDGS(에탄올 부산물의 사료 환원, 투입 옥수수의 ~1/3)는 단순화된 형태로 반영
- 공장 단위가 아닌 주 단위 집계 (개별 공장 데이터는 비공개가 많음)
- 조달단가 cᵢ의 "부족 프리미엄" 함수형은 근사 — 감도분석으로 강건성 확인

---

## 12. 기존 코드 재사용 맵

이 레포는 원래 "기후 시나리오별 작물 배치 최적화" 프로젝트였고, 주제를 피벗하면서
아래와 같이 재사용합니다.

| 기존 모듈 | 처리 |
|---|---|
| `src/model.py` (주별 수확량 ML 예측) | **개조 재사용** — 타깃·피처 구성 거의 동일 |
| `src/optimizer.py` (PuLP 주별 배분 LP) | **재설계 재사용** — 배분 구조 유사, 목적함수·제약을 6절 정식화로 교체 |
| `src/visualize.py`, 노트북 3개 | **개조 재사용** |
| `data/raw/drought_monitor.csv` | **그대로 사용** (실제 USDM 데이터) |
| `src/generate_synthetic_data.py`, 합성 수확량·기후 데이터 | **폐기** (실데이터로 교체) |

---

## 13. 다음 단계 (순서대로)

1. 팀 전원이 이 문서 리뷰 → 설계 확정
2. 데이터 확보: NASS 옥수수 생산량 + 축산 사육두수 다운로드, 파라미터 조사 시작
   (8절 표의 담당 배정)
3. 레포 리팩터: README 리프레이밍, 모듈 교체
4. 예측 모델 → LP 기본형 → 바깥층 X\* 곡선 → MIP 확장 → 2012 백테스트 → 시각화
5. 최종: 노트북·발표자료 정리
